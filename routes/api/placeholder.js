const express = require('express')
const router = express.Router()
const placeholderCtrl = require('../../controllers/api/placeholder')

// RESTful routing to Ctrl functions go here

module.exports = router